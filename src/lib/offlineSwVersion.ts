/** Bump when service worker / precache strategy changes — forces client re-register. */
export const OFFLINE_SW_VERSION = 12;

export const OFFLINE_STATIC_CACHE = `blunno-static-v${OFFLINE_SW_VERSION}`;
